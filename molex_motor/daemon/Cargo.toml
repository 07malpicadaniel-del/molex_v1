[package]
name = "molex_daemon"
version = "0.1.0"
edition = "2021"

[dependencies]
# Servidor asíncrono
tokio = { version = "1.38", features = ["full"] }

# API directa al Kernel de Linux (/dev/uinput) para saltar Wayland
evdev = "0.12.1"
